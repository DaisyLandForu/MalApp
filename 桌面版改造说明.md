# MalApp 桌面版改造说明

## 用户提出的问题

1. 当前项目需要先由开发人员启动服务，不能直接作为桌面程序使用。
2. “刷新数据”按钮缺少加载中、成功和失败反馈，用户无法判断是否生效。
3. “数据加载”按钮只有选择文件并成功解析后才启用，文件输入框又是隐藏的，容易被认为不能点击。
4. 新打包环境没有原始 `engine_detections` 表时，总览和冲突样本接口会报错。
5. 导入数据只进入待办队列，不能直接指定“加载 N 条后自动研判其中 M 条”。
6. Excel、本地 JSON/JSONL/TXT 和冲突样本没有统一到同一条自动研判流程。

## 本次解决内容

### Windows 桌面启动

- 新增 `desktop_launcher.py`。
- 双击桌面程序后自动初始化数据目录。
- 自动启动本地 HTTP 服务，不要求用户运行命令。
- 使用 Windows WebView2 内嵌桌面窗口，不再调用 Microsoft Edge 浏览器程序。
- 桌面窗口关闭后，本次服务随之退出。
- 新增本地 Qwen 开关，设置保存在 `data/model_settings.json`。
- 桌面 EXE 通过隐藏的本地模型工作进程复用 `test1/.venv` 和本机模型缓存。
- 报告详情明确显示真实 Qwen、规则模式或模型失败后的降级模式。
- 规则模式与 Qwen 模式使用不同缓存键，切换后不会复用错误模式的旧报告。

### 数据加载

- 支持选择 JSON、JSONL、TXT 文件。
- 支持直接粘贴 JSON 或 JSONL。
- 文件或粘贴内容解析后显示记录数量。
- 导入时显示进度，完成后显示成功、失败和重复数量。
- 导入数据真实写入 SQLite 的 `sample_features` 和 `sample_tasks` 表。
- 支持直接读取 `.xlsx` 文件。
- 支持选择工作表、表头所在行、数据开始行和本次传输数量。
- 大型 Excel 不会默认全量导入，用户可以自行设置 10、100、1000 等数量。

### 批次自动研判

- 每次导入建立独立批次，保存批次来源、总数及每个样本的任务状态。
- 本地 JSON、JSONL、TXT、粘贴数据、Excel 和冲突样本统一进入批次队列。
- 用户可分别设置加载数量 N 和自动研判数量 M。
- 自动研判只从选定批次读取最多 M 条待研判数据，不会混入其他历史批次。
- 后台任务逐条执行四智能体、双模型辩论和三引擎协同决策。
- 页面实时显示处理进度、当前样本、成功数、失败数和最近结果。
- 支持“数据加载后自动开始”，也支持导入后手工选择批次再启动。
- 失败任务保留为失败状态，可在同一批次中重新启动处理。
- 新增暂停和继续控制；暂停在当前样本结束后生效。
- 任务及条目进度写入 SQLite，APP 重启后可从暂停状态继续。
- APP 异常退出时，启动阶段自动将中断任务恢复为暂停状态。
- 报告记录改为永久复用；相同样本内容和相同模型模式直接返回历史报告。

### Hermes 主管编排

- 四个现有分析模块保留为 Hermes MCP 工具，没有删除静态、情报、仿冒或业务能力。
- 新增一个 Hermes 主管运行层，并行委派四个专业工具。
- 工具输出经过统一 EvidenceBlock 校验，主管不得修改工具证据。
- 原有超时、重启和降级故障配置仍由原生运行时处理，保持既有测试与回退能力。
- 当前环境无法连接 GitHub，官方 Hermes CLI 尚未安装；APP 明确显示为内嵌 Hermes MCP 兼容模式，不冒充官方运行时。

### 冲突样本刷新

- 从 `engine_detections` 中重新索引 360 与 CM 引擎评分差异或标签不一致的样本。
- 每次刷新产生独立冲突批次，可直接进入自动研判。
- 没有历史引擎表或没有冲突记录时返回 0，不创建无意义的空批次。

### 中文业务展示

- 将 `fake_app` 显示为“仿冒应用标记”。
- 将 `control_url` 显示为“控制端地址”。
- 将 `possible_variant` 显示为“疑似变种”。
- 将网络 IOC 显示为“网络威胁指标”。
- 智能体置信度改为百分比显示。
- 修复情报溯源和仿冒研判中的问号占位乱码。

### 刷新数据

- 点击后按钮显示“刷新中...”并暂时禁用，防止重复点击。
- 刷新成功后显示“数据已刷新”提示。
- 刷新失败后显示具体错误。
- 总览、任务列表、批次列表、冲突样本和引擎统计会一起刷新。

### 空数据库兼容

- 没有历史引擎数据时，页面正常显示 0，不再报表不存在。
- 空数据库仍可导入特征数据、建立任务并进行新样本研判。
- 需要使用历史 Engine A/B 数据时，可在程序关闭状态下复制原来的 `data/mvp.db`。

## 桌面版数据目录

桌面版的数据保存在程序同级的 `data` 文件夹：

```text
MalApp桌面版发布包/
  MalApp桌面版.exe
  data/
    mvp.db
    schema.json
    field_mapping.json
    sample_conflict.json
```

程序本体和数据分离的好处：

- 更新程序时不覆盖数据库。
- 复制整个文件夹即可迁移。
- 单独备份 `data` 文件夹即可保护数据。
- 不把 1.9GB 历史数据库强制打入 EXE，发布包更容易更新。

## 已完成验证

- Python 语法检查通过。
- JavaScript 语法检查通过。
- 17 项自动测试通过。
- 全新空数据库可以正常刷新。
- 全新空数据库导入 1 条数据后，特征记录和任务数量均从 0 变为 1。
- 临时数据库验证加载 3 条、自动研判 2 条后，2 条完成、1 条保持待研判。
- 真实本地 API 流程验证加载 3 条、研判 2 条，成功 2 条、失败 0 条。
- 本地 Qwen 单次生成验证成功。
- 源码服务完整单轮辩论验证为 `local_qwen`，无规则降级。
- 桌面 EXE 完整单轮辩论验证为 `local_qwen`，耗时约 387 秒，无规则降级。
- 桌面和移动页面此前已通过浏览器渲染及交互检查。

## 保留内容

- 原项目目录和原数据库不删除。
- 现有四智能体、双模型辩论、三引擎协同决策功能不删除。
- 本对话中已经列出的分析、问题和实现过程不覆盖。
- 桌面发布包输出到独立目录。
